Run the following to finish install:

sudo /root/bin/server-hostname
sudo /root/bin/server-user
sudo /root/bin/server-encryption-primary
sudo /root/bin/server-encryption-secondary (if attached another drive)

Set /etc/ssh/sshd_config.secure or disable ssh service when set!
